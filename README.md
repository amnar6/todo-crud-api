# Task Management CRUD API

A lightweight RESTful API built with Python and FastAPI that manages a to-do list in memory. 
Features interactive OpenAPI (Swagger UI) documentation, status code compliance, and input validation.

---

## How to Run

1. **Clone the repository:**
   ```bash
   git clone https://github.com/amnar6/todo-crud-api.git
   cd todo-crud-api
